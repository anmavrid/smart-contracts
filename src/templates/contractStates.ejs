  //States definition
  enum States {
    InTransition,
    <%- initial %>, <%for(var i = 0; i < states.length; i += 1) {%>
    <%-states[i].name%><%if (i < states.length-1 || finalStates.length>0) {%>,<% } %> <% } %> <%for(var i = 0; i < finalStates.length; i += 1) {%>
    <%-finalStates[i].name%><%if (i < finalStates.length-1) {%>,<% } %> <% } %>
  }
  States private state = States.<%- initial %>;
  //Insert variable definitions
